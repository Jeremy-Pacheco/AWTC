<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= shopName %> - Dashboard</title>
    <style>
      body { font-family: Arial, Helvetica, sans-serif; margin:0; padding:0; color:#111 }
      .nav { background:#fff; border-bottom:1px solid #e5e5e5; padding:12px 24px }
      .nav a { margin-right:16px; color:#333; text-decoration:none }
      .container { max-width:1100px; margin:24px auto; padding:0 24px }
      h1.site-title { display:inline-block; margin-right:24px; font-size:20px }
    </style>
  </head>
  <body>
    <div class="nav">
      <div class="container">
        <h1 class="site-title"><%= shopName %></h1>
        <a href="/">Home</a>
      </div>
    </div>

    <div class="container">
      <!-- page content will be inserted here -->
    </div>
